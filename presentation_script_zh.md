# 3-5分钟中文讲稿

大家好，我是第4组的唐志昊。下面我汇报一下这个 BraTS MRI 重建项目。

这个项目的目标是，从 AF=5 的欠采样 k-space 中重建高质量的 T2 脑部 MRI 图像。MRI 扫描时间往往比较长，如果直接降低采样率，图像里就会出现明显的混叠伪影，所以我们的任务就是在保证重建质量的前提下尽可能恢复原始图像。

整个项目分成三个部分。第一部分是欠采样仿真，我们在 k-space 里构造了随机变密度的采样 mask，然后用 FFT、mask 和逆 FFT 得到退化图像，这一步主要是为了明确模型需要去除的伪影形态。

第二部分是基线模型，我们用 U-Net 来做单模态的 T2 重建。输入是欠采样后的 T2，损失函数使用 MSE。在正式实验里，我们对数据做了病人级别划分，避免同一个病人的相邻切片泄漏到训练和测试中。同时，为了提升训练速度，我们加入了切片级缓存、pin memory、non-blocking 数据传输、channels_last 和 TF32，这样 GPU 利用率比之前更稳定。

第三部分是多模态 unrolled 模型。这个模型把欠采样 T2 和完整 T1 一起输入网络，并在每个 cascade 中加入 data consistency，强制输出结果和真实测量的 k-space 保持一致。这样做的原因是，T1 提供更稳定的结构信息，而 data consistency 可以减少网络生成不符合物理约束的结果。

从结果来看，基线模型已经能显著改善图像质量，Task 2 的 PSNR 提升到了 35.18，SSIM 提升到了 0.9271。进一步地，多模态 unrolled 模型取得了更好的结果，Task 3 的 PSNR 达到 38.12，SSIM 达到 0.9660，相比 Task 2 还有进一步提升。这说明 T1 结构信息和 data consistency 的结合确实带来了额外收益。

在误差分析中，我们也看到了少数困难切片，它们通常出现在结构更复杂的区域。但即使在这些困难样本上，重建结果也依然明显优于欠采样输入，说明模型并没有失败，而是性能有一定波动。

最后总结一下，这个项目验证了两点：第一，简单的 U-Net baseline 已经可以有效去除大部分 aliasing；第二，多模态 unrolled 设计能进一步提升定量指标和视觉质量。后续如果继续改进，可以考虑更强的边缘约束损失、更深的 cascade，或者针对困难样本做更有针对性的训练。

谢谢大家，我的汇报结束。
